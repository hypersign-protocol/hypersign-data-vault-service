---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vault-app
  namespace: hypermine-development
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vault-app
  template:
    metadata:
      labels:
        app: vault-app
    spec:
      containers:
        - name: vault-app
          image: #{GOOGLE_ARTIFACT_URL}#/#{__GOOGLE_PROJECT_ID}#/#{GOOGLE_ARTIFACT_REPO}#/vault-service:latest

          resources:
            limits:
              memory: "1Gi"
              cpu: "500m"
          ports:
            - containerPort: #{PORT}#
          readinessProbe:
            tcpSocket:
              port: #{PORT}#
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: #{PORT}#
            initialDelaySeconds: 15
            periodSeconds: 10
          env:
            - name: HOST
              value: #{HOST}#
            - name: SSL
              value: #{SSL}#
            - name: ABSOLUTE_URL
              value: #{ABSOLUTE_URL}#
            - name: DATA_VAULT
              value: #{DATA_VAULT}#
            - name: DB_URL
              value: #{DB_URL}#
            - name: PORT
              value: "#{PORT}#"
            - name: LOG
              value: #{LOG}#

---
apiVersion: v1
kind: Service
metadata:
  name: vault-service
  namespace: hypermine-development
  annotations:
    cloud.google.com/neg: '{"ingress": true}'

spec:
  type: NodePort
  selector:
    app: vault-app
  ports:
    - port: #{PORT}#
      targetPort: #{PORT}#
      protocol: TCP
---

